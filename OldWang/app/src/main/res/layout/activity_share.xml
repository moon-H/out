<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/white"
              android:gravity="center_horizontal"
              android:orientation="vertical">

    <com.whl.client.view.TitleBarView
        android:id="@+id/title_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:showBack="true"
        app:showMenu="true"/>


    <ImageView
        android:id="@+id/img_event"
        android:layout_width="@dimen/settings_invite_img_width"
        android:layout_height="@dimen/settings_invite_img_height"
        android:layout_gravity="center"
        android:scaleType="fitXY"
        />

    <Button
        android:id="@+id/btn_invite"
        android:layout_width="@dimen/settings_invite_btn_width"
        android:layout_height="@dimen/register_btn_height"
        android:layout_marginBottom="@dimen/login_remember_margin_top"
        android:layout_marginTop="@dimen/settings_invite_btn_margin_top"
        android:background="@drawable/selector_btn_pay"
        android:gravity="center"
        android:paddingBottom="@dimen/settings_invite_btn_padding"
        android:text="@string/invite_friend"
        android:textColor="@color/color_title_text"
        android:textSize="@dimen/login_btn"/>
</LinearLayout>